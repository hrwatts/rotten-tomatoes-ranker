# Legacy Scripts

These scripts preserve earlier collection approaches.

- `rt_dataset_builder.py`: discovery via the internal search endpoint
- `rt_dataset_builder_sitemap.py`: sitemap-only collection path

Use `../build_dataset.py` for the primary workflow.
